# CW-Worker-01

Change Watch worker (ID `01`).

## Run manually

1. Open **Actions** → **CW Check**
2. Click **Run workflow**
3. Enter a page `url`
4. Download the artifact (`result.json` + `screenshot.png`)

Triggered by the local runner via `workers.json` → `check.yml`.
